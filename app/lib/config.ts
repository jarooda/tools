/**
 * Global app configuration — single source of truth for app-wide constants.
 * Product name is not final yet; change APP_NAME here and it updates everywhere.
 */
export const APP_NAME = 'Toolkit'

export const APP_TAGLINE = 'Privacy-first tools that run entirely in your browser'

export const APP_DESCRIPTION =
  'A fast, privacy-first collection of tools for converting, editing, encoding, and generating — all processed locally in your browser. No uploads, no tracking.'

/** Canonical site origin, used for SEO/canonical URLs and the sitemap. */
export const SITE_URL = 'https://example.com'
